# Copy this file to app-stack.env and fill in real secrets before running
# `docker compose -f docker-compose.app.yml up -d`. Never commit the real
# app-stack.env.

POSTGRES_USER=placement_app
POSTGRES_PASSWORD=change-this-password
POSTGRES_DB=placement_test_portal

REDIS_PASSWORD=change-this-password
